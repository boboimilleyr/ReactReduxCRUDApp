import React from 'react';
import Container from '@material-ui/core/Container';

function Header() {
    return (
        <Container style={{padding: "20px"}} className="header">
            <h1>CRUD APPLICATION</h1>
        </Container>
    );
}

export default Header;
